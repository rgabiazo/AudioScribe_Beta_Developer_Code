# Import the AppManager class from the file named "file2"
from file2 import AppManager

# The following block ensures the code inside it will only be executed if this script is run directly
# and not when it's imported as a module.
if __name__ == '__main__':
    # Create an instance of the AppManager class. This will initiate the application's processes.
    manager = AppManager()


